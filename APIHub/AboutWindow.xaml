<Controls:MetroWindow x:Class="APIHub.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:APIHub"
        mc:Ignorable="d"
        Title="Pokemon GO API Hub - About" Height="200" Width="370" MinWidth="370" MinHeight="200" MaxWidth="370" MaxHeight="200" Background="#FF1C9CC1" Loaded="MetroWindow_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75*"/>
            <RowDefinition Height="95*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="257*"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="LogoIMG" HorizontalAlignment="Center" VerticalAlignment="Center" Source="Resources/icon.png"/>
        <Label x:Name="AppName" Content="PoGo Api Hub - Made by CDAGaming" Grid.Column="1" HorizontalAlignment="Right" Margin="10,10,0,0" VerticalAlignment="Top" Width="241" Foreground="White" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"/>
        <Label x:Name="Version" Content="Vx.x.x" Grid.Column="1" HorizontalAlignment="Right" Margin="0,39,0,0" VerticalAlignment="Bottom" Width="241" Foreground="White" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"/>
        <TextBox x:Name="Decription" Grid.Column="1" IsReadOnly="True" HorizontalAlignment="Right" Height="74" Margin="0,15,0,5" Grid.Row="1" TextWrapping="Wrap" Text="A PoGo Home to Leaked and User Submitted API Keys." VerticalAlignment="Center" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White" Width="251" HorizontalContentAlignment="Right"/>
    </Grid>
</Controls:MetroWindow>
